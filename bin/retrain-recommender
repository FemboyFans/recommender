#!/bin/sh

systemd-run \
  --user \
  --collect \
  --unit=retrain-recommender \
  --on-calendar=0/04:00:00 \
  -p WorkingDirectory=/var/www/recommender \
  -p CPUQuota=400% \
  bash -cl "python -m poetry run python bin/train && systemctl --user restart recommender"
