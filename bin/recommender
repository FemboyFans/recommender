#!/bin/sh

systemd-run \
  --user \
  --collect \
  --unit=recommender \
  -p Restart=always \
  -p WorkingDirectory=/var/www/recommender \
  -p EnvironmentFile=/var/www/recommender/.env \
  bash -cl "python -m poetry run gunicorn wsgi"
